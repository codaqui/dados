name: "Deploy on Raspberry Pi"

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy-streamlit:
    name: "🚀 Report Generate"
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4

    - name: "View Directory"
      run:
        echo "LS"
        ls
        echo "PWD"
        pwd
        echo "~"
        ls ~
